{
  "name": "task-hub",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/server"
  ],
  "scripts": {
    "dev": "concurrently -n web,server -c blue,green \"npm run dev -w apps/web\" \"npm run dev -w apps/server\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}

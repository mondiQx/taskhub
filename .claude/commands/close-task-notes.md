Run the `close-task-notes` skill: find recently completed tasks whose
body holds real content worth keeping, and propose routing an excerpt
into the relevant `vault/notes/` file via the usual review-queue pattern
(never write directly).

Run the `sync-calendar` skill to pull upcoming Google Calendar events into
`vault/meetings/` and create or link tasks in `vault/tasks/` for events
that genuinely need prep or follow-up. Report a short summary of events
cached and tasks created/linked/skipped.

Launch the `coding-assistant` agent to run the `run` skill: launch/start
task-hub (or screenshot it) to confirm a change actually works in the
real app, not just via tests. Report what was verified and any issues
found.

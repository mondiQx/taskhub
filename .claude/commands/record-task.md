Launch the `personal-assistant` agent to run the `record-task` skill and
turn the freeform input that follows into a single, fully-populated task
file in `vault/tasks/`. Ask before proceeding if the input is too vague to
produce a real title, due date, or priority — never invent those.

Launch the `coding-assistant` agent to run the `deploy` skill: build
task-hub for production and restart the LaunchAgent-managed server so the
phone (over Tailscale) picks up the latest code. Confirm the working
tree/branch, build, restart, smoke-test, then report what changed and
whether each step succeeded.

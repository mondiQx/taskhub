Run the `journal` skill on whatever the user just dumped (or is about to
dump) in this message — write it verbatim to today's journal first, then
propose extracted task/meeting changes via the review queue rather than
applying them silently. See the skill for the full step-by-step.
